<div class='top'>
  <h1><%= locals[:header] %></h1>

  <p>Using
    <%= link_to 'Suspenders @ https://github.com/thoughtbot/suspenders', 'https://github.com/thoughtbot/suspenders', target: "_blank" %>
  </p>

  <p>
  <%= link_to 'Requirements',
    page_path('requirements') \
    unless params[:id] == 'requirements' %>
  </p>

  <p>
  <%= link_to 'All reservations', reservations_path unless params[:action] == 'index' %>
  </p>

  <p>
  <%= link_to '>> Book a table <<',
    new_reservation_path \
    unless params[:action] == 'new' && params[:controller] == 'reservations' %>
  </p>

</div>
